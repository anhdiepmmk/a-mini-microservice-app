apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: ingress-srv
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  rules:
    - host: posts.com
      http:
        paths:
          - path: /posts/create
            backend:
              serviceName: posts-clusterip-srv
              servicePort: 8000
          - path: /posts
            backend:
              serviceName: query-srv
              servicePort: 8002
          - path: /posts/([0-9A-Za-z]+)/comments
            backend:
              serviceName: comments-srv
              servicePort: 8001
          - path: /?(.*)
            backend:
              serviceName: client-srv
              servicePort: 3000
